{
  "name": "json-fake-api",
  "version": "1.0.0",
  "description": "fake api generator",
  "main": "server.js",
  "scripts": {
    "start:fake_api": "json-server --watch fake_api.json --port 8000",
    "start:form_list": "json-server --watch form_list.json --port 8001"
  },
  "author": "acalvom",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.16.3"
  }
}
