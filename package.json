{
  "name": "json-fake-api",
  "version": "1.0.0",
  "description": "fake api generator",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 8000"
  },
  "author": "acalvom",
  "license": "ISC",
  "dependencies": {
    "faker": "^5.5.3",
    "json-server": "^0.16.3"
  }
}
